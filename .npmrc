node-linker=hoisted

# Gradle 9 refuses to snapshot a hardlink ("not a regular file"), and pnpm's
# default links from its content-addressed store. Copying costs disk; a
# native Android build that cannot run costs the session.
package-import-method=copy
