{
  "name": "nexus-hub",
  "version": "0.0.0",
  "private": true,
  "description": "Nexus \u00e2\u20ac\u201d a centralised incubator hub for building multiple ventures on shared foundations.",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "fitness": "turbo run fitness",
    "verify": "bash scripts/verify.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "pnpm": {
    "neverBuiltDependencies": [
      "esbuild"
    ],
    "//": "Security gates are fixed, never weakened (security-compliance/CLAUDE.md), so advisories reached through a transitive dep are pinned forward here rather than muted in the audit step. Each entry is a `pnpm audit --audit-level=high` finding on the patched floor the advisory names.",
    "overrides": {
      "vitest": "^3.2.6",
      "vite": "^6.4.3",
      "sharp": "^0.35.0",
      "postcss": "^8.5.18",
      "js-yaml": "^4.3.1",
      "nanoid": "^3.3.17",
      "brace-expansion": "^5.0.9",
      "glob>minimatch": "^10.0.1",
      "esbuild": "^0.25.0"
    }
  }
}
