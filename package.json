{
  "name": "nexus-hub",
  "version": "0.0.0",
  "private": true,
  "description": "Nexus â€” a centralised incubator hub for building multiple ventures on shared foundations.",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "run:all": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run.ps1",
    "run:stop": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/stop.ps1",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "fitness": "node scripts/check-folder-size.mjs && node scripts/check-docs.mjs && turbo run fitness",
    "docs:check": "node scripts/check-docs.mjs",
    "comments": "node scripts/check-comments.mjs --working",
    "parity":"node --experimental-strip-types scripts/parity/run.mjs",
    "verify": "node scripts/verify.mjs",
    "push:preflight": "node scripts/push.mjs preflight",
    "push:land": "node scripts/push.mjs land",
    "hooks:install": "simple-git-hooks",
    "prepare": "simple-git-hooks"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged",
    "commit-msg": "node scripts/check-commit-msg.mjs $1"
  },
  "lint-staged": {
    "*.{ts,tsx}": "eslint --flag v10_config_lookup_from_file --max-warnings=0",
    "*.{ts,tsx,js,jsx,mjs,cjs}": "node scripts/check-comments.mjs --staged",
    "*": "node scripts/check-staged-secrets.mjs"
  },
  "devDependencies": {
    "lint-staged": "^17.5.1",
    "simple-git-hooks": "^2.14.0",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "pnpm": {
    "neverBuiltDependencies": [
      "esbuild"
    ],
    "//": "Security gates are fixed, never weakened (security-compliance/CLAUDE.md), so advisories reached through a transitive dep are pinned forward here rather than muted in the audit step. Each entry is a `pnpm audit --audit-level=high` finding on the patched floor the advisory names.",
    "overrides": {
      "vitest": "^3.2.6",
      "vite": "^6.4.3",
      "sharp": "^0.35.4",
      "postcss": "^8.5.18",
      "js-yaml": "^4.3.2",
      "nanoid": "^3.3.17",
      "brace-expansion": "^5.0.9",
      "glob>minimatch": "^10.0.1",
      "esbuild": "^0.25.0",
      "uuid": "^11.1.1",
      "decode-uri-component": "^0.5.0"
    },
    "auditConfig": {
      "//": "image-size has NO patched version (advisories say '<0.0.0') and is reached only through Metro, Expo's bundler — a developer-machine build tool, not the shipped app or any server. Scoped to these two GHSAs so every other finding still fails the gate. Revisit when image-size publishes a fix; see pnpm-workspace.yaml.",
      "ignoreGhsas": [
        "GHSA-w3rx-r6r6-pgpr",
        "GHSA-5p2g-fcmc-qvqq"
      ]
    }
  }
}
