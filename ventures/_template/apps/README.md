# apps/

User-facing frontends for this venture — web app, marketing site, mobile, etc.
Each app is its own workspace package (picked up by `ventures/*/apps/*`).

Pull look-and-feel from `shared/packages/design-system` and `ui-kit`; talk to
the backend via `shared/packages/api-client`. Replace this file with real apps.
