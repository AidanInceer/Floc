# @shared/shared-types

Cross-cutting **TypeScript types** shared across ventures and layers — common
primitives (e.g. `Money`, `Currency`, pagination, error envelopes) and any
contracts that more than one venture or service needs. **Placeholder.**

Framework-free, no runtime code (types/interfaces/enums only where possible).
Venture-specific domain types stay in that venture, not here.
